{
  "name": "electron-markdown-editor",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",

    "clean": "rm -rf ./dist",
    "clean:win": "rm -rf ./dist/win",

    "pack": "npm run clean && npm run build:win",
    "pack:win": "npm run clean:win && electron-packager ./ \"markdown-editor\" --out=dist/win --platform=win32 --arch=ia32 --version=0.34.0 --icon=assets/icon.ico",

    "build": "npm run pack:win",
    "build:win": "npm run build:win && electron-builder \"dist/win/markdown-editor-win32\" --platform=win"
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "codemirror": "^5.10.0",
    "highlight.js": "^9.0.0",
    "jquery": "^2.1.4",
    "markdown-it": "^5.0.2",
    "markdown-it-html5-embed": "^0.3.2",
    "mathjax": "^2.5.1",
    "open": "0.0.5",
    "relative": "^3.0.2"
  }
}
